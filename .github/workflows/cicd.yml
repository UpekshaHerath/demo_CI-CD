name: demo pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Build with Maven
        run: mvn clean install -DskipTests
      - name: Login to docker hub
        run: docker login -u "${{ secrets.DOCKER_USERNAME }}" -p "${{ secrets.DOCKER_PASSWORD }}"
      - name: Build docker image
        run: docker build -t upekshaherath/demo_cicd .
      - name: Push docker image
        run: docker push upekshaherath/demo_cicd:latest

  deploy:
    needs: build
    runs-on: [aws-ec2]
    steps:
      - name: Check Docker Installation
        run: |
          if ! [ -x "$(command -v docker)" ]; then
            echo "Error: Docker is not installed." >&2
            exit 1
          fi
      - name: Pull image from docker hub
        run: sudo docker pull upekshaherath/demo_cicd:latest
      - name: Delete existing container
        run: |
          if [ "$(sudo docker ps -aq -f name=up-container)" ]; then
            sudo docker rm -f up-container
          fi
      - name: Run docker container
        run: sudo docker run -d -p 8080:8080 --name demo upekshaherath/demo_cicd
